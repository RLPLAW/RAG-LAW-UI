using ReactiveUI;

namespace UI.ViewModels;

// Ensure the ReactiveUI library is referenced in your project. 
// If it's missing, add the ReactiveUI NuGet package to your project by running:
// dotnet add package ReactiveUI

public class ViewModelBase : ReactiveObject
{
}
